<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sample.dao.MessageDao">
	<insert id="insertMessage" parameterType="Message">
		<selectKey keyProperty="messageNo" resultType="long" order="BEFORE">
			select message_seq.nextval from dual
		</selectKey>
		insert into moim_message
		(message_no,to_user_id,from_user_id,title,content)
		values
		(#{messageNo},#{toUser},#{fromUser},#{title},#{content})
	</insert>
</mapper>