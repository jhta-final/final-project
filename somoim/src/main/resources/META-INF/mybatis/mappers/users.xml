<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sample.dao.UserDao">
	<select id="getUserById" parameterType="String" resultType="User">
		select
			user_id			as  id,
			user_name 		as	name,
			tel				as	tel,
			user_password	as  password,
			nickname		as  nickname,
			email			as  email,
			birth			as  birthDate,
			gender			as  gender,
			user_content 	as  content,
			delete_yn		as  deleteYN,
			created_date	as	createdDate,
			location_no		as	locationNo,
			profile_image	as	profileImage
		from
			moim_user
		where
			user_id = #{value}
	</select>
	
	<!-- <insert id="insertUser" parameterType="User">
		insert into moim_user
			(user_id, user_name, tel, user_password, nickname, email, birth, gender, user_content, location_no, profile_image)
			values
			(#{id}, #{name}, #{tel}, #{password}, #{nickname}, #{email}, #{birth}, #{gender}, #{content), #{locationNo}, #{profileImage})
	</insert> -->

</mapper>