<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sample.dao.WarningDao">
	<select id="getAllWarnings" resultType="Warning">
		select
			warning_no		as warningNo,
			user_id			as userId,
			login_user_id	as loginUserId,
			content			as content,
			type			as type,
			type_no			as typeNo,
			created_date	as createdDate,
			delete_yn		as deleteYN
		from
			moim_warning
	</select>
	
	<insert id="insertWarning" parameterType="Warning">
		<selectKey keyProperty="warningNo" resultType="long" order="BEFORE">
			select warning_seq.nextval from dual
		</selectKey>
		insert into moim_warning
		(warning_no,user_id,login_user_id,content,type,type_no)
		values
		(#{warningNo},#{userId},#{loginUserId},#{content},#{type},#{typeNo})
	</insert>
	
	<update id="updateWarning" parameterType="long">
		update moim_warning
		set
			delete_yn = 'Y'
		where
			warning_no = #{value}
	</update>
	
	<select id="getWarningsByUserId" parameterType="string" resultType="Warning">
		select
			warning_no		as warningNo,
			user_id			as userId,
			login_user_id	as loginUserId,
			content			as content,
			type			as type,
			type_no			as typeNo,
			created_date	as createdDate,
			delete_yn		as deleteYN
		from
			moim_warning
		where
			user_id = #{value}
	</select>
</mapper>